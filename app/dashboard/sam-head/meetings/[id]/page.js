// SAM Head uses the same MOM detail page as SAM Executive
// The component handles role-based back links and permissions internally
export { default } from '@/app/dashboard/sam-executive/meetings/[id]/page';
