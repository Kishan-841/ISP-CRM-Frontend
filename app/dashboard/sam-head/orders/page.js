// SAM Head uses the same orders page as SAM Executive
// The component handles role-based filtering and customer fetching internally
export { default } from '@/app/dashboard/sam-executive/orders/page';
